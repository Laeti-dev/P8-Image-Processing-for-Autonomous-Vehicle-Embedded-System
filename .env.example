# Copy this file to .env and fill in your values.
# cp .env.example .env

# =============================================================================
# Model Configuration (for Streamlit app and predictor)
# =============================================================================

# Model source: "local" or "azure"
# MODEL_SOURCE=local

# Local model path (when MODEL_SOURCE=local)
# Example: models/best_model.keras or /absolute/path/to/model.keras
# MODEL_PATH=models/best_model.keras

# =============================================================================
# Azure Blob Storage (for training uploads and model download)
# =============================================================================

# Option 1: Connection string (recommended)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net

# Option 2: Account name + key separately
# AZURE_STORAGE_ACCOUNT_NAME=your_account_name
# AZURE_STORAGE_ACCOUNT_KEY=your_account_key

# Azure container name (default: training-outputs)
# AZURE_CONTAINER_NAME=training-outputs

# Blob name for the model (when loading from Azure)
# Examples: model/best_model.keras, MobileNetV2/model/global_best_model.keras
# AZURE_MODEL_BLOB_NAME=model/best_model.keras
